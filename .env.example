# OpenTofu Environment Configuration
# Copy to .env: cp .env.example .env
#
# This file uses op:// references that are resolved by direnv's use_op() function.
# You must run 'op signin' before entering this directory.
#
# Required 1Password items (Infrastructure vault):
#   - Proxmox: Login with url, token_id, credential fields
#   - env-arr-stack: Secure Note with .env content for arr stack
#   - service-account: Item with credential field (service account token)

# Proxmox credentials (from 1Password)
TF_VAR_proxmox_api_url=op://Infrastructure/Proxmox/strongmad/url
TF_VAR_proxmox_api_token_id=op://Infrastructure/Proxmox/token_id
TF_VAR_proxmox_api_token_secret=op://Infrastructure/Proxmox/credential
TF_VAR_proxmox_tls_insecure=true

# Proxmox node and storage (customize for your setup)
TF_VAR_vm_storage=local-lvm
TF_VAR_snippet_storage=local
TF_VAR_vm_bridge=vmbr0

# VM user configuration
TF_VAR_vm_user=kringler
