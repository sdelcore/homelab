# Proxmox connection (generate with `just init-secrets`)
# proxmox = {
#   api_url          = "https://proxmox.local:8006"
#   api_token_id     = "terraform@pve!terraform-token"
#   api_token_secret = "your-token-secret"
#   tls_insecure     = true
# }

# pfSense connection (generate with `just init-secrets`)
# pfsense = {
#   url             = "https://10.0.0.1"
#   username        = "admin"
#   password        = "your-password"
#   tls_skip_verify = true
# }

vm = {
  storage = "local-lvm"
  bridge  = "vmbr0"
}

ssh_public_keys = [
  "ssh-ed25519 AAAA...",
]
